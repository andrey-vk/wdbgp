version: "2"

linters:
  default: none
  enable:
    - staticcheck
    - govet
    - errcheck
    - ineffassign
    - unused
    - revive
    - gosec
    - gocritic
    - misspell

  settings:
    errcheck:
      check-type-assertions: true
      check-blank: true
    revive:
      rules:
        - name: blank-imports
        - name: context-as-argument
        - name: error-return
        - name: error-strings
        - name: receiver-naming

  exclusions:
    generated: lax
    rules:
      - path: _test\.go
        linters:
          - errcheck
        text: "Error return value of .* is not checked"
      - path: _test\.go
        linters:
          - gosec
        text: "G404"
      - path: internal/web/i18n\.go
        linters:
          - gosec
        text: "G101"
      - path: runtime/
        linters:
          - misspell
      - path: internal/bgp/peer\.go
        linters:
          - errcheck
        text: "Error return value of .*(Close|Write|SetDeadline).* is not checked"

formatters:
  enable:
    - gofmt
    - goimports
